# キャラクターファイル規約

## ルール

- 新規キャラは `_template.md` のフォーマットをコピーして作成する
- ファイル名は `{役割}_{名前}.md`（例: `protagonist_yuuki.md`, `heroine_saki.md`）
- キャラ追加時は `backbone/relationships.md` も必ず更新する
- 口調・話し方セクションには具体的なセリフ例を3つ以上含める
- `role` タグは以下から選ぶ: protagonist, heroine, antagonist, ally, mentor, rival, mob
